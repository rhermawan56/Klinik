<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.praktek.klinik">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_local_hospital"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_local_hospital"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Load">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Create" />
        <activity android:name=".Menu" />
        <activity android:name=".RumahSakitActivity" />
        <activity android:name=".BookingDokterActivity" />
        <activity
            android:name=".InfoAkunActivity"
            android:label="@string/title_activity_info_akun"
            android:theme="@style/AppTheme"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>